Back-end:

CHECK) Controllo sull'aggiunta delle immagini per auto(DA TESTARE)
    Impostato in maniera tale che se elimini l'ultima immagine allora viene eliminato l'intero optional ed è
    da reinserire

CHECK)  Optional di tipo COLORE, con prezzo = 0 non variano il tempo di consegna

1) Eliminare sei sicuro di volerti disconnettere, quando facciamo login

2) Revisione codice e implementazione testing

Front - end:
1) Implementazione viewOptional per customAuto:
    modifica dei singoli componenti optional
    motore
    dimensione
    bottoni

2) Finire implementazione per addModel e addOptional

3)Implementazione controlli su bottoni e campi di input di tutte le pagine!(forse meglio in un unica classe esterna)

4) Implementazione messaggio popUp quando usciamo da aggiungiModello
BUGS:
